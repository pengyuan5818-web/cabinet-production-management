### 3.8 门板喷涂模块（文档8）

#### 3.8.1 门板生产表 (door_panel_production)

```sql
-- ============================================
-- 门板生产表
-- ============================================
CREATE TABLE door_panel_production (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    order_id UUID NOT NULL REFERENCES order_master(id),
    order_detail_id UUID REFERENCES order_detail(id),
    
    door_no VARCHAR(50) NOT NULL UNIQUE,
    door_type VARCHAR(20) NOT NULL,
    
    length INT, width INT, thickness DECIMAL(5,2), weight DECIMAL(8,2), quantity INT DEFAULT 1,
    material VARCHAR(100), color VARCHAR(50), finish VARCHAR(50),
    
    has_handle BOOLEAN DEFAULT FALSE, handle_type VARCHAR(50), handle_position VARCHAR(50),
    has_hinge BOOLEAN DEFAULT TRUE, hinge_count INT,
    
    coating_type VARCHAR(50), coating_color VARCHAR(50), coating_brand VARCHAR(50), coating_thickness DECIMAL(5,2),
    baking_temp DECIMAL(5,2), baking_time INT,
    
    status VARCHAR(50) DEFAULT 'pending',
    cutting_start DATETIME, cutting_complete DATETIME,
    pretreat_start DATETIME, pretreat_complete DATETIME,
    coating_start DATETIME, coating_complete DATETIME,
    baking_start DATETIME, baking_complete DATETIME,
    quality_start DATETIME, quality_complete DATETIME,
    
    quality_result VARCHAR(20), quality_photos JSONB, quality_remark TEXT,
    package_type VARCHAR(50), edge_protection VARCHAR(50),
    current_location VARCHAR(100),
    
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_door_panel_order ON door_panel_production(order_id);
```

#### 3.8.2 喷涂工艺配方表 (coating_recipe)

```sql
CREATE TABLE coating_recipe (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    recipe_code VARCHAR(50) NOT NULL UNIQUE,
    recipe_name VARCHAR(100) NOT NULL,
    door_type VARCHAR(20), coating_type VARCHAR(50), color VARCHAR(50),
    pretreat_temp DECIMAL(5,2), pretreat_time INT, degrease_temp DECIMAL(5,2), degrease_time INT,
    spray_pressure DECIMAL(5,2), spray_distance DECIMAL(5,2), spray_speed VARCHAR(20), spray_pass INT,
    baking_temp DECIMAL(5,2), baking_time INT, baking_ramp VARCHAR(20),
    primer_type VARCHAR(50), primer_thickness DECIMAL(5,2), topcoat_type VARCHAR(50), topcoat_thickness DECIMAL(5,2),
    notes TEXT, is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, created_by UUID
);
```

#### 3.8.3 喷涂设备表 (coating_equipment)

```sql
CREATE TABLE coating_equipment (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    equipment_code VARCHAR(50) NOT NULL UNIQUE, equipment_name VARCHAR(100) NOT NULL,
    equipment_type VARCHAR(50) NOT NULL,
    location VARCHAR(100), model VARCHAR(100), manufacturer VARCHAR(100),
    status VARCHAR(20) DEFAULT 'running', today_output INT DEFAULT 0, total_output INT DEFAULT 0,
    last_maintenance DATE, next_maintenance DATE, maintenance_cycle INT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.8.4 喷涂BOM表 (coating_bom)

```sql
CREATE TABLE coating_bom (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    coating_type VARCHAR(20) NOT NULL, item_name VARCHAR(100) NOT NULL, unit VARCHAR(20),
    consumption_per_unit DECIMAL(10,4), supplier_id UUID, unit_price DECIMAL(12,2)
);
```

---

### 3.9 仓库管理模块

#### 3.9.1 仓库表 (warehouse)

```sql
CREATE TABLE warehouse (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    warehouse_code VARCHAR(50) NOT NULL UNIQUE, warehouse_name VARCHAR(100) NOT NULL,
    warehouse_type VARCHAR(20) DEFAULT 'main', address VARCHAR(500), area DECIMAL(10,2),
    manager_id UUID, status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.9.2 库位表 (warehouse_location)

```sql
CREATE TABLE warehouse_location (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    warehouse_id UUID NOT NULL REFERENCES warehouse(id),
    location_code VARCHAR(50) NOT NULL, location_name VARCHAR(100),
    zone VARCHAR(50), shelf VARCHAR(50), layer INT, position INT,
    status VARCHAR(20) DEFAULT 'available',
    UNIQUE(warehouse_id, location_code), created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.9.3 物料表 (material)

```sql
CREATE TABLE material (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    material_code VARCHAR(50) NOT NULL UNIQUE, material_name VARCHAR(200) NOT NULL,
    material_type VARCHAR(50) NOT NULL,
    spec VARCHAR(100), unit VARCHAR(20), color VARCHAR(50), brand VARCHAR(100),
    supplier_id UUID, unit_price DECIMAL(12,2), safety_stock DECIMAL(10,2) DEFAULT 0,
    max_stock DECIMAL(10,2), material_status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_material_code ON material(material_code);
CREATE INDEX idx_material_type ON material(material_type);
```

#### 3.9.4 库存表 (inventory)

```sql
CREATE TABLE inventory (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    warehouse_id UUID NOT NULL REFERENCES warehouse(id),
    material_id UUID NOT NULL REFERENCES material(id),
    location_id UUID REFERENCES warehouse_location(id),
    batch_no VARCHAR(50), quantity DECIMAL(10,2) DEFAULT 0,
    available_quantity DECIMAL(10,2) DEFAULT 0, locked_quantity DECIMAL(10,2) DEFAULT 0,
    unit_cost DECIMAL(12,2), total_cost DECIMAL(12,2),
    expire_date DATE, quality_status VARCHAR(20) DEFAULT 'qualified',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(warehouse_id, material_id, batch_no)
);

CREATE INDEX idx_inventory_warehouse ON inventory(warehouse_id);
CREATE INDEX idx_inventory_material ON inventory(material_id);
```

#### 3.9.5 库存预警表 (inventory_alert)

```sql
CREATE TABLE inventory_alert (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    material_id UUID NOT NULL REFERENCES material(id),
    alert_type VARCHAR(20) NOT NULL,
    threshold DECIMAL(10,2), current_quantity DECIMAL(10,2),
    alert_level VARCHAR(20) DEFAULT 'warning',
    status VARCHAR(20) DEFAULT 'pending',
    handled_by UUID, handled_at DATETIME, remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.9.6 出入库记录表 (inventory_transaction)

```sql
CREATE TABLE inventory_transaction (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    transaction_no VARCHAR(50) NOT NULL UNIQUE,
    biz_type VARCHAR(20) NOT NULL, biz_id UUID, biz_no VARCHAR(50),
    warehouse_id UUID NOT NULL REFERENCES warehouse(id),
    material_id UUID NOT NULL REFERENCES material(id),
    location_id UUID REFERENCES warehouse_location(id),
    in_quantity DECIMAL(10,2) DEFAULT 0, out_quantity DECIMAL(10,2) DEFAULT 0,
    before_quantity DECIMAL(10,2), after_quantity DECIMAL(10,2),
    unit_cost DECIMAL(12,2), total_cost DECIMAL(12,2),
    batch_no VARCHAR(50), expire_date DATE,
    from_order_id UUID, from_order_no VARCHAR(50),
    operator_id UUID, operator_name VARCHAR(100),
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_inventory_transaction_biz ON inventory_transaction(biz_type, biz_id);
```

#### 3.9.7 供应商表 (supplier)

```sql
CREATE TABLE supplier (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    supplier_code VARCHAR(50) NOT NULL UNIQUE, supplier_name VARCHAR(200) NOT NULL,
    supplier_type VARCHAR(50), contact_person VARCHAR(100), phone VARCHAR(20),
    email VARCHAR(100), address VARCHAR(500),
    bank_name VARCHAR(100), bank_account VARCHAR(50), tax_no VARCHAR(50),
    credit_level VARCHAR(20), status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 3.10 物流安装模块

#### 3.10.1 物流记录表 (logistics_record)

```sql
CREATE TABLE logistics_record (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    logistics_no VARCHAR(50) NOT NULL UNIQUE,
    order_id UUID NOT NULL REFERENCES order_master(id),
    logistics_company VARCHAR(100), logistics_no_ext VARCHAR(100),
    driver_name VARCHAR(100), driver_phone VARCHAR(20), driver_id VARCHAR(20),
    send_province VARCHAR(50), send_city VARCHAR(50), send_district VARCHAR(50), send_address VARCHAR(500),
    receive_province VARCHAR(50), receive_city VARCHAR(50), receive_district VARCHAR(50),
    receive_address VARCHAR(500), receive_contact VARCHAR(100), receive_phone VARCHAR(20),
    freight DECIMAL(10,2), insured_amount DECIMAL(12,2),
    status VARCHAR(20) DEFAULT 'pending',
    package_count INT, total_weight DECIMAL(8,2),
    ship_date DATE, estimate_arrive DATE, actual_arrive DATETIME,
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_logistics_order ON logistics_record(order_id);
```

#### 3.10.2 物流追踪表 (logistics_track)

```sql
CREATE TABLE logistics_track (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    logistics_record_id UUID NOT NULL REFERENCES logistics_record(id),
    track_time DATETIME NOT NULL, location VARCHAR(200),
    status VARCHAR(50), description TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.10.3 安装任务表 (installation_task)

```sql
CREATE TABLE installation_task (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_no VARCHAR(50) NOT NULL UNIQUE,
    order_id UUID NOT NULL REFERENCES order_master(id),
    logistics_id UUID REFERENCES logistics_record(id),
    install_province VARCHAR(50), install_city VARCHAR(50), install_district VARCHAR(50),
    install_address VARCHAR(500), install_contact VARCHAR(100), install_phone VARCHAR(20),
    appointment_date DATETIME, appointment_remark TEXT,
    leader_id UUID, leader_name VARCHAR(100),
    status VARCHAR(20) DEFAULT 'pending',
    actual_start DATETIME, actual_complete DATETIME, actual_duration INT,
    accept_status VARCHAR(20), accept_photos JSONB, accept_remark TEXT, accept_date DATETIME,
    visit_status VARCHAR(20), visit_date DATETIME, visit_remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.10.4 安装人员分配表 (installer_allocation)

```sql
CREATE TABLE installer_allocation (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id UUID NOT NULL REFERENCES installation_task(id),
    employee_id UUID, employee_name VARCHAR(100), role VARCHAR(20),
    work_date DATE, work_hours DECIMAL(5,2), overtime_hours DECIMAL(5,2),
    allowance DECIMAL(10,2), created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.10.5 安装进度表 (installation_progress)

```sql
CREATE TABLE installation_progress (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id UUID NOT NULL REFERENCES installation_task(id),
    progress INT, stage VARCHAR(50), stage_name VARCHAR(100),
    description TEXT, photos JSONB,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.10.6 安装验收表 (installation_accept)

```sql
CREATE TABLE installation_accept (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id UUID NOT NULL REFERENCES installation_task(id),
    accept_type VARCHAR(20),
    self_check_result VARCHAR(20), self_check_by UUID, self_check_at DATETIME,
    factory_check_result VARCHAR(20), factory_check_by UUID, factory_check_at DATETIME,
    customer_sign_result VARCHAR(20), customer_sign_photos JSONB, customer_sign_date DATETIME,
    issue_list JSONB, remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.10.7 安装回访表 (installation_visit)

```sql
CREATE TABLE installation_visit (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id UUID NOT NULL REFERENCES installation_task(id),
    visit_type VARCHAR(20), visit_date DATE,
    satisfaction INT, quality_score INT, service_score INT,
    feedback TEXT, issue_list JSONB, handle_status VARCHAR(20),
    visitor_id UUID, visitor_name VARCHAR(100),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 3.11 包装管理模块

#### 3.11.1 包装记录表 (package_record)

```sql
CREATE TABLE package_record (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    package_no VARCHAR(50) NOT NULL UNIQUE,
    order_id UUID NOT NULL REFERENCES order_master(id),
    package_type VARCHAR(50), package_method VARCHAR(50),
    total_packages INT, total_quantity INT, total_weight DECIMAL(8,2),
    longest_length INT, total_volume DECIMAL(10,2),
    status VARCHAR(20) DEFAULT 'pending',
    scan_operator_id UUID, scan_operator_name VARCHAR(100), scan_time DATETIME,
    storage_area VARCHAR(50), storage_position VARCHAR(50),
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_package_order ON package_record(order_id);
```

#### 3.11.2 分拣规则表 (sort_rule)

```sql
CREATE TABLE sort_rule (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    rule_code VARCHAR(50) NOT NULL UNIQUE, rule_name VARCHAR(100) NOT NULL,
    rule_type VARCHAR(20), conditions JSONB,
    assigned_area VARCHAR(50), assigned_position VARCHAR(50),
    priority INT DEFAULT 0, is_active BOOLEAN DEFAULT TRUE, remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.11.3 包装类型规则表 (package_type_rule)

```sql
CREATE TABLE package_type_rule (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    product_type VARCHAR(50) NOT NULL, package_type VARCHAR(50) NOT NULL,
    material VARCHAR(200), thickness DECIMAL(5,2),
    protective_material VARCHAR(100), edge_protection VARCHAR(100),
    label_required VARCHAR(200), weight_limit DECIMAL(8,2),
    is_active BOOLEAN DEFAULT TRUE, remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.11.4 包装物料明细表 (package_item)

```sql
CREATE TABLE package_item (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    package_id UUID NOT NULL REFERENCES package_record(id),
    product_name VARCHAR(200), product_type VARCHAR(50),
    quantity INT, weight DECIMAL(8,2), length INT, width INT, height INT,
    scanned BOOLEAN DEFAULT FALSE, scanned_at DATETIME, scanned_by UUID,
    barcode VARCHAR(100), remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_package_item_package ON package_item(package_id);
CREATE INDEX idx_package_item_barcode ON package_item(barcode);
```

---

### 3.12 财务模块

#### 3.12.1 收款登记表 (payment_in)

```sql
CREATE TABLE payment_in (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    payment_no VARCHAR(50) NOT NULL UNIQUE,
    source_type VARCHAR(20) NOT NULL, source_id UUID, source_no VARCHAR(50),
    amount DECIMAL(12,2) NOT NULL, actual_amount DECIMAL(12,2),
    customer_id UUID REFERENCES customer(id), customer_name VARCHAR(100),
    dealer_id UUID REFERENCES dealer(id),
    payment_method VARCHAR(20), payment_account VARCHAR(100),
    bank_name VARCHAR(100), bank_account VARCHAR(50), bank_water VARCHAR(100),
    voucher_file VARCHAR(500),
    status VARCHAR(20) DEFAULT 'pending', confirmed_by UUID, confirmed_at DATETIME,
    order_id UUID REFERENCES order_master(id), order_no VARCHAR(50),
    receivable_id UUID, remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, created_by UUID
);

CREATE INDEX idx_payment_in_source ON payment_in(source_type, source_id);
CREATE INDEX idx_payment_in_customer ON payment_in(customer_id);
```

#### 3.12.2 付款登记表 (payment_out)

```sql
CREATE TABLE payment_out (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    payment_no VARCHAR(50) NOT NULL UNIQUE,
    source_type VARCHAR(20) NOT NULL, source_id UUID, source_no VARCHAR(50),
    amount DECIMAL(12,2) NOT NULL, actual_amount DECIMAL(12,2),
    supplier_id UUID REFERENCES supplier(id), supplier_name VARCHAR(200),
    payment_method VARCHAR(20), payment_account VARCHAR(100),
    bank_name VARCHAR(100), bank_account VARCHAR(50), bank_water VARCHAR(100),
    voucher_file VARCHAR(500),
    status VARCHAR(20) DEFAULT 'pending', confirmed_by UUID, confirmed_at DATETIME,
    purchase_id UUID, purchase_no VARCHAR(50),
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, created_by UUID
);
```

#### 3.12.3 发票管理表 (invoice)

```sql
CREATE TABLE invoice (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    invoice_no VARCHAR(50) NOT NULL UNIQUE, invoice_type VARCHAR(20) NOT NULL,
    tax_rate DECIMAL(5,2), tax_amount DECIMAL(12,2), total_amount DECIMAL(12,2),
    buyer_name VARCHAR(200), buyer_tax_no VARCHAR(50), buyer_address VARCHAR(500),
    buyer_bank VARCHAR(100), buyer_bank_account VARCHAR(50),
    seller_name VARCHAR(200), seller_tax_no VARCHAR(50), seller_address VARCHAR(500),
    seller_bank VARCHAR(100), seller_bank_account VARCHAR(50),
    invoice_content JSONB,
    source_type VARCHAR(20), source_id UUID, source_no VARCHAR(50),
    invoiced_by UUID, invoiced_at DATETIME, dealer_id UUID REFERENCES dealer(id),
    status VARCHAR(20) DEFAULT 'pending', remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.12.4 应收账款表 (receivable)

```sql
CREATE TABLE receivable (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    receivable_no VARCHAR(50) NOT NULL UNIQUE,
    source_type VARCHAR(20) NOT NULL, source_id UUID, source_no VARCHAR(50),
    total_amount DECIMAL(12,2) NOT NULL, received_amount DECIMAL(12,2) DEFAULT 0, pending_amount DECIMAL(12,2),
    customer_id UUID REFERENCES customer(id), customer_name VARCHAR(100), dealer_id UUID REFERENCES dealer(id),
    payment_terms INT, due_date DATE, overdue_days INT DEFAULT 0,
    status VARCHAR(20) DEFAULT 'pending', payment_ids JSONB,
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.12.5 应付账款表 (payable)

```sql
CREATE TABLE payable (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    payable_no VARCHAR(50) NOT NULL UNIQUE,
    source_type VARCHAR(20) NOT NULL, source_id UUID, source_no VARCHAR(50),
    total_amount DECIMAL(12,2) NOT NULL, paid_amount DECIMAL(12,2) DEFAULT 0, pending_amount DECIMAL(12,2),
    supplier_id UUID REFERENCES supplier(id), supplier_name VARCHAR(200),
    payment_terms INT, due_date DATE, overdue_days INT DEFAULT 0,
    status VARCHAR(20) DEFAULT 'pending', payment_ids JSONB,
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.12.6 经销商应收款表 (dealer_receivable)

```sql
CREATE TABLE dealer_receivable (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    receivable_no VARCHAR(50) NOT NULL UNIQUE, dealer_id UUID NOT NULL REFERENCES dealer(id),
    source_type VARCHAR(20) NOT NULL, source_id UUID, source_no VARCHAR(50),
    amount DECIMAL(12,2) NOT NULL, paid_amount DECIMAL(12,2) DEFAULT 0, pending_amount DECIMAL(12,2),
    payment_terms INT, due_date DATE, overdue_days INT DEFAULT 0,
    status VARCHAR(20) DEFAULT 'pending', payment_ids JSONB, remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.12.7 经销商付款表 (dealer_payment)

```sql
CREATE TABLE dealer_payment (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    payment_no VARCHAR(50) NOT NULL UNIQUE, dealer_id UUID NOT NULL REFERENCES dealer(id),
    payment_type VARCHAR(20) NOT NULL, source_id UUID, source_no VARCHAR(50),
    amount DECIMAL(12,2) NOT NULL, payment_method VARCHAR(20),
    bank_name VARCHAR(100), bank_account VARCHAR(50), bank_water VARCHAR(100), voucher_file VARCHAR(500),
    status VARCHAR(20) DEFAULT 'pending', confirmed_by UUID, confirmed_at DATETIME,
    receivable_id UUID REFERENCES dealer_receivable(id), remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, created_by UUID
);
```

#### 3.12.8 经销商佣金表 (dealer_commission)

```sql
CREATE TABLE dealer_commission (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    commission_no VARCHAR(50) NOT NULL UNIQUE, dealer_id UUID NOT NULL REFERENCES dealer(id),
    order_id UUID REFERENCES dealer_order(id), order_no VARCHAR(50),
    order_amount DECIMAL(12,2), commission_rate DECIMAL(5,2), commission_amount DECIMAL(12,2),
    status VARCHAR(20) DEFAULT 'pending',
    settled_amount DECIMAL(12,2) DEFAULT 0, settled_at DATETIME, settled_by UUID,
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 3.13 OA模块

#### 3.13.1 员工表 (employee)

```sql
CREATE TABLE employee (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_no VARCHAR(50) NOT NULL UNIQUE, name VARCHAR(100) NOT NULL,
    phone VARCHAR(20), email VARCHAR(100),
    dept_id UUID, dept_name VARCHAR(100), position_id UUID, position_name VARCHAR(100),
    id_card VARCHAR(20), entry_date DATE, contract_end_date DATE,
    bank_name VARCHAR(100), bank_account VARCHAR(50),
    status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.13.2 考勤记录表 (attendance)

```sql
CREATE TABLE attendance (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_id UUID NOT NULL REFERENCES employee(id), employee_name VARCHAR(100),
    work_date DATE NOT NULL,
    check_in_time DATETIME, check_out_time DATETIME,
    work_hours DECIMAL(5,2), overtime_hours DECIMAL(5,2),
    status VARCHAR(20) DEFAULT 'normal',
    source VARCHAR(20) DEFAULT 'manual', device_no VARCHAR(50),
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(employee_id, work_date)
);

CREATE INDEX idx_attendance_employee ON attendance(employee_id);
```

#### 3.13.3 请假申请表 (leave)

```sql
CREATE TABLE leave_application (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    leave_no VARCHAR(50) NOT NULL UNIQUE,
    employee_id UUID NOT NULL REFERENCES employee(id), employee_name VARCHAR(100),
    leave_type VARCHAR(20) NOT NULL,
    start_date DATE NOT NULL, end_date DATE NOT NULL, total_days DECIMAL(5,1),
    reason TEXT, attachment JSONB,
    status VARCHAR(20) DEFAULT 'pending',
    approval_id UUID, approval_no VARCHAR(50), approval_status VARCHAR(20),
    approved_by UUID, approved_at DATETIME, approved_remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.13.4 报销记录表 (expense)

```sql
CREATE TABLE expense (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    expense_no VARCHAR(50) NOT NULL UNIQUE,
    employee_id UUID NOT NULL REFERENCES employee(id), employee_name VARCHAR(100),
    expense_type VARCHAR(20) NOT NULL, amount DECIMAL(12,2) NOT NULL,
    expense_date DATE NOT NULL, expense_content TEXT, attachment JSONB,
    status VARCHAR(20) DEFAULT 'pending',
    approval_id UUID, approval_no VARCHAR(50),
    approved_by UUID, approved_at DATETIME, approved_remark TEXT,
    reimbursed BOOLEAN DEFAULT FALSE, reimbursed_at DATETIME, reimbursed_by UUID,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.13.5 加班记录表 (overtime)

```sql
CREATE TABLE overtime_record (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    overtime_no VARCHAR(50) NOT NULL UNIQUE,
    employee_id UUID NOT NULL REFERENCES employee(id), employee_name VARCHAR(100),
    work_date DATE NOT NULL, start_time DATETIME, end_time DATETIME, total_hours DECIMAL(5,2),
    reason TEXT, status VARCHAR(20) DEFAULT 'pending',
    approved_by UUID, approved_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 3.14 系统模块

#### 3.14.1 部门表 (department)

```sql
CREATE TABLE department (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    dept_code VARCHAR(50) NOT NULL UNIQUE, dept_name VARCHAR(100) NOT NULL,
    parent_id UUID, dept_level INT DEFAULT 1, manager_id UUID,
    status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.14.2 职位表 (position)

```sql
CREATE TABLE position (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    position_code VARCHAR(50) NOT NULL UNIQUE, position_name VARCHAR(100) NOT NULL,
    dept_id UUID REFERENCES department(id), position_level INT DEFAULT 1,
    status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.14.3 操作日志表 (operation_log)

```sql
CREATE TABLE operation_log (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    log_no VARCHAR(50) NOT NULL UNIQUE,
    module VARCHAR(50), action VARCHAR(50),
    operator_id UUID, operator_name VARCHAR(100), operator_ip VARCHAR(50), operator_device VARCHAR(100),
    biz_type VARCHAR(50), biz_id UUID, biz_no VARCHAR(50),
    before_value JSONB, after_value JSONB,
    remark TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_operation_log_biz ON operation_log(biz_type, biz_id);
CREATE INDEX idx_operation_log_operator ON operation_log(operator_id);
```

---

## 四、表清单汇总

| # | 表名 | 模块 | 说明 |
|---|------|------|------|
| 1 | dealer | 经销商 | 经销商主表 |
| 2 | dealer_user | 经销商 | 经销商用户 |
| 3 | dealer_permission | 经销商 | 权限配置 |
| 4 | dealer_application | 经销商 | 入驻申请 |
| 5 | customer | 客户 | 客户主表 |
| 6 | customer_follow | 客户 | 客户跟进 |
| 7 | dealer_customer | 客户 | 经销商客户 |
| 8 | dealer_customer_follow | 客户 | 经销商客户跟进 |
| 9 | design_record | 设计 | 设计记录 |
| 10 | design_attachment | 设计 | 设计附件 |
| 11 | dealer_design_file | 设计 | 经销商设计文件 |
| 12 | quote | 报价 | 报价主表 |
| 13 | quote_detail | 报价 | 报价明细 |
| 14 | approval_center | 审批 | 统一审批中心 |
| 15 | order_master | 订单 | 订单主表 |
| 16 | order_detail | 订单 | 订单明细 |
| 17 | order_tracking | 订单 | 订单追踪 |
| 18 | dealer_order | 订单 | 经销商订单 |
| 19 | cabinet_board | 阿尔法家 | 拆单板件 |
| 20 | order_bom | 阿尔法家 | 订单BOM |
| 21 | alpha_import_log | 阿尔法家 | 导入记录 |
| 22 | alpha_download_log | 阿尔法家 | 下载记录 |
| 23 | countertop_production | 台面 | 台面生产 |
| 24 | countertop_bom | 台面 | 台面BOM |
| 25 | door_panel_production | 门板 | 门板生产 |
| 26 | coating_recipe | 门板 | 喷涂配方 |
| 27 | coating_equipment | 门板 | 喷涂设备 |
| 28 | coating_bom | 门板 | 喷涂BOM |
| 29 | warehouse | 仓库 | 仓库 |
| 30 | warehouse_location | 仓库 | 库位 |
| 31 | material | 仓库 | 物料 |
| 32 | inventory | 仓库 | 库存 |
| 33 | inventory_alert | 仓库 | 库存预警 |
| 34 | inventory_transaction | 仓库 | 出入库记录 |
| 35 | supplier | 仓库 | 供应商 |
| 36 | logistics_record | 物流 | 物流记录 |
| 37 | logistics_track | 物流 | 物流追踪 |
| 38 | installation_task | 安装 | 安装任务 |
| 39 | installer_allocation | 安装 | 人员分配 |
| 40 | installation_progress | 安装 | 安装进度 |
| 41 | installation_accept | 安装 | 安装验收 |
| 42 | installation_visit | 安装 | 安装回访 |
| 43 | package_record | 包装 | 包装记录 |
| 44 | sort_rule | 包装 | 分拣规则 |
| 45 | package_type_rule | 包装 | 包装类型规则 |
| 46 | package_item | 包装 | 包装物料 |
| 47 | payment_in | 财务 | 收款登记 |
| 48 | payment_out | 财务 | 付款登记 |
| 49 | invoice | 财务 | 发票管理 |
| 50 | receivable | 财务 | 应收账款 |
| 51 | payable | 财务 | 应付账款 |
| 52 | dealer_receivable | 财务 | 经销商应收款 |
| 53 | dealer_payment | 财务 | 经销商付款 |
| 54 | dealer_commission | 财务 | 经销商佣金 |
| 55 | employee | OA | 员工表 |
| 56 | attendance | OA | 考勤记录 |
| 57 | leave_application | OA | 请假申请 |
| 58 | expense | OA | 报销记录 |
| 59 | overtime_record | OA | 加班记录 |
| 60 | department | 系统 | 部门 |
| 61 | position | 系统 | 职位 |
| 62 | operation_log | 系统 | 操作日志 |

**总计：62张核心数据表**

---

## 五、统一枚举值

```sql
-- 生产阶段
CREATE TYPE production_stage AS ENUM (
    'order_confirmed','design_confirmed','alpha_import',
    'material_prepared','cutting','bending','welding','polishing',
    'edge_banding','drilling','assembly',
    'countertop_production','countertop_quality',
    'door_panel_production','door_panel_quality',
    'quality_check','packaging','warehouse_out',
    'logistics_shipped','installation','completed'
);

-- 客户状态
CREATE TYPE customer_status AS ENUM ('new','following','ordered','completed');

-- 订单状态
CREATE TYPE order_status AS ENUM ('draft','pending','producing','shipped','installed','completed','cancelled');

-- 经销商状态
CREATE TYPE dealer_status AS ENUM ('pending','active','suspended','terminated');

-- 审批状态
CREATE TYPE approval_status AS ENUM ('pending','approved','rejected','cancelled');

-- 台面类型
CREATE TYPE countertop_type AS ENUM ('filled','solid','rock');

-- 门板类型
CREATE TYPE door_panel_type AS ENUM ('stainless','paintable','wood');

-- 涂层类型
CREATE TYPE coating_type AS ENUM ('powder','spray','laminate');

-- 仓库业务类型
CREATE TYPE inventory_biz_type AS ENUM (
    'purchase_in','purchase_return','production_in','production_out',
    'sales_out','sales_return','adjustment','inventory_count'
);
```

---

## 六、编号规则

| 业务 | 前缀 | 示例 |
|------|------|------|
| 客户 | KH | KH2026040001 |
| 经销商 | JXS | JXS001 |
| 订单 | DD | DD2026040001 |
| 报价 | BJ | BJ2026040001 |
| 设计 | SJ | SJ2026040001 |
| 审批 | SP | SP2026040001 |
| 采购入库 | RK | RK2026040001 |
| 采购出库 | CK | CK2026040001 |
| 包装 | BZ | BZ2026040001 |
| 安装 | AZ | AZ2026040001 |
| 物流 | WL | WL2026040001 |
| 导入 | IMP | IMP20260417001 |

---

*文档版本: V3.0*
*更新日期: 2026-04-17*
*整合完成*
